{{- if .Values.global.useExternalSecrets }}
{{- range $k, $v := .Values.secrets }}  
  {{ include "harness-manager.externalSecret" (dict "secret" $k "shared" "true" "context" $) }}
  --- 
{{- end }}
{{- end }}

{{/* 
{{ include "harness-manager.externalSecret" (dict "secret" "APPDYNAMICS_AGENT_ACCOUNT_ACCESS_KEY" "shared" "true" "context" $) }}
---
{{ include "harness-manager.externalSecret" (dict "secret" "AWS_MARKETPLACE_ACCESSKEY" "shared" "true" "context" $) }}
---
{{ include "harness-manager.externalSecret" (dict "secret" "LOG_STREAMING_SERVICE_TOKEN" "shared" "true" "context" $) }}
*/}}